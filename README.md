To run scraper (run.py is available in the `apps/` directory for each language separately): 

`python run.py --token=<your_github_token> --repos-num=<repos-num> --year=<year>`

c++ main scraper function: https://github.com/JakubZelek/CFG_ETL/blob/master/apps/cpp_scraper/generate_cfg.py \
python main scraper function: https://github.com/JakubZelek/CFG_ETL/blob/master/apps/python_scraper/generate_cfg.py

#TODO: fix the issue for kafka part, bitnami repo for images is not longer supported.
